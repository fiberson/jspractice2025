/* 
Максим, создай функцию createUser(firstName, lastName), которая возвращает объект с методами и полями:

firstName - имя

lastName - фамилия

getFirstName() — возвращает имя.

getLastName() — возвращает фамилию.

getFullName() — возвращает имя и фамилию с пробелом.
*/

const user = createUser('Илон', 'Маск')

console.log(user.getName()) // Илон